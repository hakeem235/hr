/**
 * Seeded KSA benefit plans — config-as-data.
 * Plans are HR-editable via the API; these are defaults for a new entity.
 */
import type { BenefitPlan } from './types.js';
export declare const DEFAULT_PLANS: BenefitPlan[];
