{
  "name": "@hr/workflow-sagas",
  "version": "0.1.0",
  "description": "Durable saga workers for onboarding and offboarding — Temporal-pattern saga engine",
  "type": "module",
  "main": "dist/src/index.js",
  "scripts": {
    "dev":   "node --import tsx/esm src/index.ts",
    "build": "tsc -b",
    "test":  "node --import tsx/esm --test test/domain.test.ts"
  },
  "dependencies": { "fastify": "^4.26.0" },
  "devDependencies": { "tsx": "^4.0.0", "typescript": "^5.4.0" }
}
