{
  "name": "@hr/leave",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Leave module — reference implementation (CLAUDE.md §6)",
  "main": "src/index.ts",
  "scripts": {
    "dev": "node --import tsx/esm src/index.ts",
    "test": "node --import tsx/esm --test test/domain.test.ts",
    "build": "tsc -b"
  },
  "dependencies": {
    "fastify": "^4.26.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.0",
    "tsx": "^4.0.0",
    "typescript": "^5.4.0"
  }
}
